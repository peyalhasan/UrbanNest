import React from 'react';

const Cart = ({carts}) => {
    return (
        <div>
            <h1 className='text-xl font-bold' >Here is your added product</h1>
            {
                carts.map(cart =>)
            }
        </div>
    );
};

export default Cart;